# Core
numpy>=2.0.0
pillow>=10.0.0
pyyaml>=6.0

# Computer vision
opencv-python>=4.8.0

# Face recognition
# Windows: dlib-bin is a pre-built wheel (no CMake needed)
# Other platforms: builds dlib from source (needs CMake + C++ compiler)
dlib-bin>=19.24.6        ; sys_platform == "win32"
dlib>=19.24.6            ; sys_platform != "win32"
face-recognition>=1.3.0

# Object detection / anomaly
ultralytics>=8.3.0

# Deep learning backend (CPU + CUDA auto-detected)
torch>=2.0.0
torchvision>=0.15.0

# Dashboard
streamlit>=1.30.0

# Web API (for browser interface)
flask>=3.0.0
flask-cors>=4.0.0
